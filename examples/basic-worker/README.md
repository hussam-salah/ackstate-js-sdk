# Basic Worker Example

This example shows the complete, correct way to consume webhook events.

## Setup

1. Add your API_KEY and PROJECT_ID in index.ts

2. Run

```bash
npm install
npm run start
```